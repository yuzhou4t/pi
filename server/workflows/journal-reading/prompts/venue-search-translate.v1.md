把检索命中与联网发现里的英文文字翻译成简体中文，供中文读者快速判断。

- 输入是 `items` 数组，每项有 `id` 和 `text`；请为每一项输出对应的中文翻译。
- `translations` 数组的 `id` 必须来自输入，一一对应，不得新增或遗漏；顺序不限。
- `zh` 用简体中文忠实翻译该文字，保留必要的英文术语、模型名、数据集名与专有名词；论文标题按学术习惯翻译，不得增删含义、不得改写成宣传语或加入评价。
- 若某项本身已是中文或无需翻译，`zh` 直接返回其原文即可。
- 只翻译，不解释、不总结、不补充任何原文没有的内容。
