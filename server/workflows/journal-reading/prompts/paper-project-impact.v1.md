根据当前项目状态和论文题录，为每篇论文写一条简洁、可核验的项目作用说明。

- `impacts` 必须覆盖输入 `papers` 中的每个 `request_id`，一一对应，不增不减。
- `project_impact` 使用简体中文，只说明这篇论文可能帮助项目完成什么具体判断、设计或验证。
- 只能依据输入中的项目目标、决定、开放问题、下一步、论文标题、摘要和主题标签。
- 不得编造论文结论；题录证据不足时明确写“待全文核验”。
- 不复述整段摘要，不写泛泛的“有参考价值”，不加入输入之外的新项目目标。
