阶段：research-question。

仅依据输入中的论文证据，先说明研究背景和既有方法留下的具体矛盾，再说明作者真正提出的研究问题与贡献主张。把已由正文支持的内容和仍待核验的主张分开表达。

要求：

- `answer` 用简洁中文完成“领域定位 → 背景 → Gap → 研究问题/贡献”的连贯解释；
- `evidence[].locator` 必须逐字使用输入允许的 `block_id`；
- `evidence[].support` 说明该原文块具体支持哪一项判断；
- 不读取或推断项目上下文；
- 没有证据时明确写“待核验”，不要补充常识或论文外事实。
