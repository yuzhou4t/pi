# Pi Agent 产品讨论记录

> 日期：2026-07-22  
> 状态：论文闭环与项目工作会话前端合同均已确认，进入 V1 纵向切片实现。

## 产品定位

Pi Agent 是一个**以长期项目为中心的个人工作流播放器**。

- 项目是长期主体，保存目标、历史决定、开放问题和下一步。
- 工作流是推进项目的可复用方法，Run 是一次具体执行。
- 用户看到的是完整工作流；脚本、工具、模型和 Skill 默认收在运行详情中。
- Codex 负责创造、修改和修复工作流；Pi Agent 负责周期性、低摩擦地播放已经验证的工作流。
- 两者的差别不是模型选择，也不是代码与内容的差别。

产品应当“外部以项目为中心，内部以工作流为运行核心”，不做通用聊天壳，也不在 V1 扩张成个人 AI 操作系统或工作流市场。

2026-07-25 的确认补充了第二种同级工作表面：工作流继续负责可重复、可恢复的项目推进；正常工作会话负责普通的 Agent 任务。它既可以绑定长期项目，也可以作为无需先选文件夹的一次性独立对话开始；两者都不要求把开放任务伪装成一条论文工作流。

## 首个闭环

首个工作流为：

```text
周期监测
→ 脚本标准化、去重和规则过滤
→ AI 审阅少量候选
→ 本周重点候选
→ 五分钟导读
→ 用户决定只收藏或进入分阶段精读
→ 为选择精读的论文完成四个阅读阶段
→ 生成 Zotero / Obsidian 归档草稿
→ 生成项目状态更新建议
→ 对全部外部写入进行精确预览
→ 用户一次确认所选内容后顺序提交
```

默认监测 CCF A 类人工智能方向的 4 个期刊和 7 个会议，围绕 Agent、规划与工具、记忆与上下文、RAG、人机确认、评估与可靠性六类主题。每轮最多推荐两篇：一篇推进当前项目，一篇拓展前沿视野。

脚本负责收集、游标、标准化、去重、规则过滤和基础热度信号；AI 负责语义相关性、推荐解释、导读、精读和项目影响草稿。每轮尽量扫描全部 11 个来源，只有本周发表才算新；不足 5 篇时从稳定经典池补齐，完全没有时返回 5 篇经典回顾，并明确标注。脚本把候选压缩到最多 8 篇再调用一次模型。最终 5 篇都会下载 PDF 并尝试提交 MinerU Cloud，用户随后仍只选择最多 2 篇进入导读与精读。

完整实现合同见 [V1 期刊追踪与精读规格](./docs/v1-journal-reading-workflow.md)。

## 成果与事实源

- Zotero 保存题录、PDF、五分钟导读和 Obsidian 链接。
- Obsidian 保存一篇论文对应的一篇可持续补充的完整精读笔记。
- 项目状态 Markdown 只保存已确认的项目结论、决定、开放问题、下一步和引用。
- Pi Agent 自身只保存游标、缓存、草稿和 Run 历史，不成为长期知识事实源。

项目目录中的人类可读 Markdown 是项目状态事实源；未来的数据库只做索引、缓存和运行记录。

## 权限与确认

- 已绑定项目范围内的读取默认允许。
- Pi Agent 自己的临时缓存和运行记录可以自动写入。
- Zotero、Obsidian 和项目文件的正式写入必须先展示精确预览。
- 用户可以逐项勾选或取消，然后一次确认所选内容。
- “可以”“开始精读”“选择论文”不等于写入授权。
- 确认出现在当前 Run 中，右侧不设置常驻待确认队列。

## 精读体验

每篇论文先展示五分钟导读，再由用户决定是否进入精读。精读按以下阶段逐步进行：

1. 研究问题；
2. 方法机制；
3. 实验证据；
4. 与项目的关系。

用户可以逐阶段追问、修正和暂停，最后再合成 Obsidian 主笔记。周报只负责索引和串联，不把多篇论文混成一份长期笔记。

## 信息架构

```text
文件夹
└── 项目
    ├── 工作流
    │   ├── 当前 Run
    │   └── 历史 Run
    └── 多个对话 / 干预记录
```

- 左栏组织项目和 Run。
- 中栏展示当前 Run 的唯一主任务、成果或确认预览。
- 右栏以“当前依据 / 项目状态”两个互斥视图展示来源证据和已确认项目记录。
- Skills 入口保留在右上角，设置保留在左下角。

V1 不提供通用工作流编辑器。第一个工作流可以在内部组合项目理解、论文筛选、MinerU、论文精读、Zotero、Obsidian 写入和收尾能力，但用户默认只看到“期刊追踪与精读”这一条完整流程。

## 项目工作会话（2026-07-25，已确认）

本节覆盖上方将“多个对话”仅作为干预记录、以及把中栏定义为 Run 唯一主任务的旧口径。新的复用外壳固定为：

```text
工作类型 → 项目与会话
→ 始终可对话的 Agent
→ 当前文件 / 更改 / 预览 / 运行结果
```

- 左上先选择 `正常工作` 或 `论文精读`，再展示和管理该类型下的项目与会话。这覆盖此前“只在新建会话面板选择类型、两类会话在同一项目树混排”的决定；切换工作类型时分别恢复各自最后选择的项目、会话和审阅状态。
- 每种工作类型下，左栏仍采用接近 Codex 的“多个项目 → 项目内多个会话”树。正常工作中的“选择本地文件夹”与“新建项目文件夹”已经接入 macOS 原生选择器；浏览器只持有安全标签，canonical root 只保存在本机服务端。
- 正常工作的左栏在项目树之前提供一级“新建对话”和“独立对话”分组。独立对话不是隐藏项目：创建时不打开文件夹选择器、不扫描或复制用户目录，也不启动 Pi；它以 `projectId: null` 持久化并恢复。
- 独立对话只有服务端私有草稿区，不能访问用户本地文件夹，但仍可使用 Pi 的计划、受控文件工具、Diff、哈希绑定确认和有界验证。确认只写入该草稿区；删除只清理该会话及草稿，不影响任何项目。
- 独立对话与项目会话的创建、列表、重命名和删除接口保持作用域隔离；不支持静默把独立对话转换成项目会话。未来若增加绑定或移动，必须明确展示文件访问范围变化。
- `工作流 Run` 是项目下相关的后台执行与状态，不是第二个论文研读会话；不得把一次期刊活动同时作为 Run 和同名会话重复展示。
- 项目工作会话的中栏展示任务、三步计划、折叠活动、确认状态和完成结果。桌面端默认让中栏占满除左栏外的可用空间，右栏工件默认关闭，并通过一个明确按钮一键打开或关闭；开关工件不得重置对话。
- 左栏收起只保留一个壳层级按钮，由同一个左上控制承担收起与重新展开，不再同时显示栏内和工作区中的两个镜像按钮。
- 项目工作不再由“修复设置页移动端底部按钮遮挡”固定 fixture 驱动。创建空会话只写轻量元数据；用户显式提交任意项目任务后，Pi 从真实项目的受控只读视图读取，并只在会话级稀疏审阅层中准备修改。右栏展示服务端重算的逐文件 Diff，用户确认后才写回所选文件，验证按 attempt 保留失败与重试证据。
- 文件和代码选区只作为可移除上下文加入输入框；打开页面、绑定项目、切换会话、切换工件或切换模型都不会调用模型。
- “更改”工件同时承载精确 diff、文件选择、目标哈希和确认按钮。确认会绑定 change-set 与逐文件哈希，在写前复核真实项目并读回核验；取消或未选择不会推进，也不会执行 Git 操作。
- 项目工作 runtime 使用 Pi SDK 的持久会话、事件、steer、abort、retry 与 compact，但只装载项目内受控文件工具和结构化计划/验证工具，不暴露 Bash、插件、Skill 或任意系统命令。
- 验证命令必须由 Pi 建议、用户在右栏显式点击；只有此时才从已确认的真实项目物化一次性副本并执行。界面显示最终 package script 和本机用户权限边界。它不是 PTY，也不宣称具备容器级隔离。
- 会话行使用单一 `…` 菜单提供重命名与永久删除。第一条显式消息可确定性生成短标题，重命名不调用模型；删除必须二次确认并明确只清理会话记录、计划、运行证据和未应用草稿，不删除项目文件夹或回滚已确认写入。活动中的 Agent、验证或应用操作先阻止删除，不自动中止。
- 本轮不做没有恢复入口的假归档。删除成功后优先切换到同项目下一条会话，再回退上一条；项目没有会话时保留空状态，不自动创建替代会话。
- 该切片借鉴 PI WEB 的项目—工作区—会话层级与 Codex 的任务—计划—差异审阅节奏，但保持 Pi Agent 的暖中性色、深青强调和项目工作流身份。

完整合同见 [V1 项目工作会话可点击原型规格](./docs/v1-project-workbench-prototype.md)。

## 当前代码状态

- React/Vite 桌面外壳、设置、服务商菜单和空 Skill 中心已经存在。
- 项目工作会话前端合同与论文会话并列：项目内可创建、切换和恢复普通 Agent 工作会话，并在右侧查看文件、更改、预览和运行证据。
- 正常工作已接入真实本地服务：项目注册、Pi SDK 会话、真实项目的受控只读视图、稀疏审阅层、安全文件工具、计划/活动事件、哈希绑定写回和可重试验证记录均可持久恢复。
- 首个前端纵向切片已经可以完整操作：5 篇候选、最多选择 2 篇、双论文逐篇决定、四阶段精读、追问、三处写入预览、部分失败和失败项续跑。
- 合法状态迁移由 reducer 控制，演示 Run 使用版本化 `localStorage` 跨刷新恢复。
- 论文写入和项目修改的确认都与当前精确预览共置；右栏不成为跨会话的常驻确认队列。
- 本地回环服务、双模型适配、持久化 Run Store、11 个来源扫描、经典兜底、PDF 下载和 MinerU Cloud 后端已经接入。
- 真实五分钟导读和四阶段精读已经接入，阶段草稿、引用、追问、修正与阅读位置可恢复。
- Zotero 受控提案/写入切片已经接入；Obsidian 主笔记的精确创建预览与项目状态文件的精确追加预览也已经实现，均绑定目标与内容哈希，并且只生成 Run Store 产物，不改动正式文件。
- 三个目标的一次批量批准与真实联合写入尚未实现，定时调度也尚未完成。界面不会把精确预览伪装成已经正式写入。
- 当前产品与验收范围只覆盖桌面端；移动端布局与移动视口 QA 不再作为本阶段要求。项目工件内的移动页面宽度预览仍用于审阅项目本身，不代表 Pi Agent 提供移动端界面。

## 下一实现顺序

1. ✅ 用 fixture 数据跑通候选审阅、导读、精读、写入预览和恢复状态。
2. ✅ 接本地 Node.js 服务、Run Store 和只读项目状态绑定。
3. ✅ 接真实来源扫描与轻量结构化语义初筛。
4. ✅ 基于 MinerU Markdown 接导读与分阶段精读。
5. **当前：**三组精确预览已经接通；继续实现一次批量批准、可恢复的顺序提交，以及 Obsidian 和项目状态的真实受控写入。
6. 用真实一周窗口试运行，记录质量、Token、人工时间和失败恢复结果。

## 尚未配置

- 首个真实项目目录和项目状态文件路径。
- Obsidian 精读笔记目录。
- Zotero 目标 collection。
- 每周运行时间。
- 首次基线模型。
- 11 个来源的官方入口和 fallback 注册表。

这些属于项目绑定参数，不再影响产品定位。
