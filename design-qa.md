# Pi Agent 极简可读性调整：设计 QA

> 验收日期：2026-07-22  
> 状态：`review_ready / 本周待审阅`

## 视觉事实源与规格化

- 用户反馈事实源：默认字号过小、候选信息过密、右栏内容混在一起，不符合极简方向。
- 桌面调整前截图：`/private/tmp/pi-agent-minimal-audit-03-desktop.png`（1440 × 1024 px）。
- 移动调整前截图：`/private/tmp/pi-agent-minimal-audit-01-current.png`、`/private/tmp/pi-agent-minimal-audit-02-evidence.png`（均为 400 × 672 px）。
- 桌面最终实现：`/private/tmp/pi-agent-minimal-after-desktop.png`（1440 × 1024 px，CSS 视口 1440 × 1024，DPR 1）。
- 移动最终实现：`/private/tmp/pi-agent-minimal-after-mobile-400x672.png`、`/private/tmp/pi-agent-minimal-after-evidence-400x672.png`（400 × 672 px，CSS 视口 400 × 672，DPR 1）。
- 展开与项目状态：`/private/tmp/pi-agent-minimal-interactions-desktop.png`、`/private/tmp/pi-agent-minimal-mobile-project-state.png`。
- 桌面全视图对照：`/private/tmp/pi-agent-minimal-comparison-desktop.png`。
- 桌面中心与右栏聚焦对照：`/private/tmp/pi-agent-minimal-focus-center.png`、`/private/tmp/pi-agent-minimal-focus-context.png`。
- 移动候选与依据对照：`/private/tmp/pi-agent-minimal-mobile-candidate-compare.png`、`/private/tmp/pi-agent-minimal-mobile-evidence-compare.png`。
- 候选摘要调整后：`/private/tmp/pi-agent-candidate-summary-desktop.png`（1440 × 1024 px）、`/private/tmp/pi-agent-candidate-summary-mobile.png`（400 × 672 px）。
- 候选摘要前后对照：`/private/tmp/pi-agent-candidate-summary-desktop-compare.png`、`/private/tmp/pi-agent-candidate-summary-mobile-compare.png`。
- 同组对照均使用相同视口、相同候选审阅状态和相同 DPR，无需密度归一化。

## 可见对照结论

- 中心区从“每篇默认展开全部标签、热度和证据”改为两层摘要；默认依次回答“论文讲什么”和“对项目的作用”，再通过“查看依据”渐进披露次要信息。同时最多展开一篇依据。
- `86 / 17 / 5` 三格指标改为一行扫描摘要，避免形成小型仪表盘。
- 右栏从一条长堆栈拆为“当前依据 / 项目状态”两个互斥 Tab。当前依据只显示当前论文、证据范围和最多三条相关证据；项目状态分为决定、开放问题和下一步。
- 桌面主标题为 21 px，阶段标题 20 px，论文标题 15 px，正文 13 px，元信息最低 12 px；移动论文标题 16 px、正文 15 px、元信息 13 px。
- 移动端主操作和依据展开按钮为 44 px 高；页面宽度和 `scrollWidth` 均为 400 px，无横向溢出。

## 必查表面

- 字体与排版：继续使用 `Noto Sans SC / PingFang SC / Microsoft YaHei` 回退链；字号、行高和字重形成明确的标题、正文、元信息三级关系，不再用 8–10 px 承载主要内容。
- 间距与布局：保留三栏结构和暖中性表面，但减少默认可见层数；候选之间用单一分隔而非多层卡片。移动端单次只显示项目、本轮或依据中的一个视图。
- 颜色与 Token：沿用 `#0f766e` 深青强调和暖灰背景；强调色只用于主行动、选中状态与证据提示，未新增装饰性色。
- 图像与素材：本工具界面不需要照片或插画；可见图标均来自 Phosphor，没有 Emoji、手工 SVG、CSS 绘图或占位图。
- 文案与内容：扫描摘要、候选决定、证据范围和项目状态分工清楚；演示边界仍明确，未暗示真实写入已完成。
- 交互与可访问性：依据展开、右栏 Tabs、候选选择、移动底部导航均为语义控件；Tabs 支持方向键、Home/End；焦点样式和 reduced-motion 规则保留。

## 比较历史与修复

### 第 1 轮：桌面信息密度

- [P1] 主要内容普遍为 8–10 px，论文标题、正文与证据几乎同一灰度层级。
  - 修复：整体字体阶梯上调，正文颜色加深，并同步增加行高与候选纵向节奏。
- [P1] 每篇论文的热度和证据默认展开，右栏把依据、来源、运行记录和项目状态一次性并列。
  - 修复：候选依据改为单项渐进披露；右栏改为两个互斥 Tab，并删除重复热度堆叠。
- [P2] 三个扫描数字被表现为指标卡，流程步骤和元信息零件过多。
  - 修复：扫描统计合并为一句；Run 顶部移除 Run ID 和多余图标，只保留时间窗与状态。

### 第 2 轮：移动断点

- [P1] 工作流右栏的后置基础样式覆盖了移动隐藏规则，导致“本轮”和“依据”在同一页面连续出现。
  - 修复：移动断点显式设置 `.workflow-context-rail` 隐藏，仅在 `is-mobile-active` 时显示。
- [P2] 五步标签被压成微型圆点文字，粘性操作栏下方可看到被遮住的候选内容。
  - 修复：移动端只视觉显示步骤编号，步骤名称保留为可访问隐藏文本；操作栏改为不透明背景并消除底部 padding 穿透。

### 第 3 轮：最终复核

- 桌面、中心聚焦、右栏聚焦及两组 400 × 672 移动对照中未发现仍需修复的 P0、P1 或 P2。
- 没有需要阻塞交付的 P3 项。

### 第 4 轮：候选摘要层级

- 用户事实源：进入精读前要先知道论文在讲什么，同时保留它对当前项目的作用。
- 修复：候选正文改为无卡片、无边框的语义化两行信息；“论文讲什么”使用短选择摘要，“对项目的作用”保留原相关性判断，热度和证据仍放在展开区。
- 桌面与 400 × 672 移动对照中未发现新增的 P0、P1 或 P2；窄屏采用纵向阅读，不为追求首屏塞入而压缩字号。

### 第 5 轮：模型接线前的状态合同

- 审计发现第 3–5 篇候选可以被选择，但没有对应导读内容，会进入空白导读页。复现截图：`/private/tmp/pi-agent-audit-04-empty-guide.png`。
- 修复：没有下游导读合同的论文保持不可选；reducer 同样拒绝非法选择；恢复本地状态时会丢弃包含此类论文的旧 Run。
- 修复：写入提案根据本轮实际决定重新收敛。只收藏导读时，预览和完成页只显示 Zotero，不再暗示 Obsidian 或项目状态已写入。
- 修复：400 × 672 下将运行标题与模型信息拆为上下两行，并把粘性操作栏改为“说明在上、按钮在下”，不再把说明压成单字窄列。
- 当前截图：`/private/tmp/pi-agent-model-slice-desktop-final.png`（桌面候选）、`/private/tmp/pi-agent-model-slice-guide.png`（非空导读）、`/private/tmp/pi-agent-model-slice-mobile-fixed.png`（400 × 672 移动候选）。

### 第 6 轮：本地模型接入

- GPT 订阅登录已通过本机 Codex CLI 探测并完成真实候选说明调用；DeepSeek 无可用 Key 时会明确禁用。
- 模型菜单只显示服务状态和模型，不暴露 Key、接口地址或 CLI 参数。
- 页面加载、刷新与模型切换不再自动产生调用；候选区使用显式“用当前模型更新”动作。
- Vite 与本地模型服务只监听回环地址；Codex 执行禁用工具、插件、Skills、浏览器、计算机控制和工作区依赖。

### 第 7 轮：真实全文阅读与 Zotero 分流

- 5 篇真实候选均可在不改变选择状态的前提下打开全文；候选页明确区分“浏览全文”和后续“只收藏 / 进入精读”。
- 中栏提供逐段、全文、目录和原始 PDF 四个入口。逐段位置使用稳定章节/段落锚点，不伪造 PDF 页码；全文只保留一条操作说明，点击任意段落即可回到对应逐段位置，避免重复操作文案铺满页面。
- 原始题录标题优先于 MinerU OCR 标题；右栏把“全文已解析”和“尚未完成精读核验”分开说明，不再显示已经过时的准备状态或技术流程名。
- 桌面 1280 × 720 与移动 390 × 844 实测均无横向溢出。移动端逐段/全文、返回、PDF 和段落导航保持 44 px 核心触控目标；页面运行日志无错误或警告。
- PDF 端点已实测返回 `Accept-Ranges: bytes`，`bytes=0-4` 返回 `206` 与有效 `%PDF-` 文件头；不属于当前 Run 的论文读取返回 `404 PAPER_NOT_FOUND`。

## 功能与构建验收

- 最新全量回归（2026-07-23）：`npm test` 共 122 项通过；`npm run build` 通过；Vite 未出现错误覆盖层。
- 实际完成：选两篇 → 准备导读 → 第一篇进入精读、第二篇只收藏 → 四阶段推进 → 生成三处写入预览。
- 实际完成：模拟 Obsidian 失败 → 进入部分失败 → 只重试失败项 → 完成。
- 候选依据可展开/收起且同时最多展开一篇；右栏桌面与移动端均可切换“当前依据 / 项目状态”。
- 移动候选页正文为 15 px、标题 16 px、核心按钮 44 px；移动本轮与依据不会同时显示。

## 当前边界

当前已经具备候选说明的双服务商桥接：GPT 复用本机 Codex 的 ChatGPT 订阅登录，DeepSeek 使用独立服务端 API Key；浏览器只接触允许的 provider/model ID。来源扫描、Run Store、最终 5 篇 PDF 下载、MinerU Cloud 批处理以及真实逐段/全文/PDF 只读阅读已经接入；真实项目读取、AI 导读/精读生成以及 Zotero、Obsidian、项目文件写入仍未接入。

真实试跑对照：未节流时只有 2/11 来源成功；改为单路节流并加入 DBLP 官方镜像回退后达到 11/11，扫描 775 条题录、识别 21 条主题相关历史发现，最终 5 篇 PDF 全部下载并通过校验。配置 `PI_MINERU_API_TOKEN` 后复用同一 Run 和 PDF 缓存完成真实 MinerU Cloud 联调，5 篇均生成 Markdown 与图片产物，Run 回到 `review_ready`。联调中补齐了 5 篇并行上传、API/上传/结果包超时、批次恢复和已落盘结果不降级保护。

final result: passed

## 项目树与会话入口修正（2026-07-25）

### 视觉事实源与规格化

- 用户反馈事实源：左栏应像 Codex 一样直接展示多个项目及其会话；新建会话入口属于项目行；“正常工作 / 论文研读”只在创建流程中选择；期刊追踪是后台活动，不是第二个研读会话。
- 调整前截图：`/private/tmp/pi-agent-left-rail-before.png`（1280 × 720 px，CSS 视口 1280 × 720，DPR 1）。
- 同状态最终实现：`/private/tmp/pi-agent-project-tree-final-1280.png`（1280 × 720 px，CSS 视口 1280 × 720，DPR 1）。
- 桌面与中等宽度：`/private/tmp/pi-agent-left-rail-after-1440.png`（1440 × 1024 px）、`/private/tmp/pi-agent-project-tree-final-1180.png`（1180 × 844 px）。
- 移动项目树与创建面板：`/private/tmp/pi-agent-project-tree-final-mobile-390.png`、`/private/tmp/pi-agent-new-conversation-final-mobile-390.png`（均为 390 × 844 px）。
- 调整前与 1280 × 720 最终实现已在同一个比较输入中并列查看；两者像素尺寸、CSS 视口和 DPR 一致，无需密度归一化。

### 比较历史与修复

- [P1] 调整前把“会话”和“后台运行”分成两个标题区；“论文研读”与“期刊追踪与精读”看起来像同一活动被重复并换位。
  - 修复：移除两个分组标题；会话直接嵌套在项目下；Run 收敛为低层级的“本周追踪”状态行。
- [P1] “修复设置页移动端遮挡”紧跟“会话”标题，容易被理解为项目工作能力入口。
  - 修复：每个项目行新增明确的 `+` 新建会话按钮；固定任务只保留为一个已有会话标题。
- [P1] 左栏只有一个项目层级，无法验证多项目与会话恢复。
  - 修复：加入第二个项目及独立持久化工作会话；切换项目会恢复各自标题、状态和当前工件。
- [P2] 工作/研读类型同时出现在导航层和 Run 层，切换位置不清楚。
  - 修复：类型选择只出现在“新建会话”面板；正常工作进入项目 Agent，论文研读进入本周追踪的论文选择。
- [P2] 旧持久化运行证据缺少日志数组时会让工作台空白。
  - 修复：持久化校验拒绝不完整记录，运行工件也提供安全的空日志回退。
- [P2] 移动端创建面板的关闭按钮仍为 30 px。
  - 修复：移动端关闭按钮扩大到 44 × 44 px；创建按钮保持 48 px 高。

### 必查表面

- 字体与排版：沿用现有中文无衬线界面字体和标题衬线字体；项目名、会话名与状态副标题保持三级层级，没有引入微型主文案。
- 间距与布局：保留 240 px 左栏、约 460 px Agent 中栏和最大工件区；项目行、嵌套引导线、会话行及追踪行保持单一树形节奏。
- 颜色与 Token：继续使用暖象牙背景、暖白表面和深青选中态；新建面板没有引入新的装饰色或渐变。
- 图像与素材：界面只使用现有 Phosphor 图标；没有新增图片、Emoji、手工 SVG、CSS 绘图或占位资产。
- 文案与内容：项目行显示会话/追踪数量；会话副标题明确为“正常工作”或“论文研读”；追踪活动不再使用与论文会话竞争的完整流程名称。
- 交互与可访问性：项目选择、会话选择、项目行新建按钮、两种会话类型和创建/取消均为语义控件；移动项目加号、关闭和创建操作不小于 44 px。

### 浏览器与功能证据

- IAB 页面身份为 `Pi Agent`，首个有意义界面正常渲染，无框架错误覆盖层；新开页面控制台 `warn/error` 为 0。
- 交互路径已验证：项目一 → 项目行 `+` → 正常工作 → 创建“新会话” → 项目计数由 2 增为 3 → 刷新后恢复该会话；项目二仍保留独立工作会话。
- 1180 px 下 `documentElement.scrollWidth = clientWidth = 1180`；390 × 844 下项目树和底部导航没有页面级横向溢出。
- 论文入口只导航到本周追踪选择，不在打开面板、切换类型或取消时调用模型。

### 最终结论

- 同视口前后对照未发现仍需修复的 P0、P1 或 P2。
- 仍保留的产品边界：前端会话创建与状态恢复使用本地 fixture；真实目录索引、自由任务 Agent runtime 和文件写入属于后端替换阶段。

final result: passed

## 项目工作会话 V1：设计 QA（2026-07-25）

### 视觉事实源与同视口对照

- 结构事实源：`qa/agent-center-mock.html`，固定左栏项目/会话、中栏 Agent、右栏当前工件的职责。
- 外部参考：`qa/source-pi-web.png` 仅用于核对项目—工作区—会话的信息层级，不复制其紫色、终端密度或控制台视觉。
- 1440 × 1024 参考截图：`/private/tmp/pi-agent-source-mock-1440.png`。
- 1440 × 1024 最终实现：`/private/tmp/pi-agent-project-workbench-1440.png`。
- 390 × 844 最终实现：`/private/tmp/pi-agent-project-workbench-mobile-agent.png`、`/private/tmp/pi-agent-project-workbench-mobile-artifact.png`。
- 参考与实现已经在同一个比较输入中并排复核；最终实现保持约 240 px 左栏、460 px Agent 中栏和占据剩余宽度的工件栏。

### 可见对照结论

- 三栏职责与参考一致，但内容已替换为项目工作合同：左栏以项目树混排正常工作与论文研读会话，追踪只作为项目活动；中栏显示任务、三步计划、折叠活动和结果；右栏显示文件、更改、预览或运行证据。
- 暖象牙背景、暖白卡片和深青强调沿用既有设计系统；未引入 PI WEB 紫色、Codex 渐变欢迎页、自由终端或 IDE 密度。
- “更改”把文件选择、unified diff、增删统计、基础/目标哈希与确认按钮放在同一工件；“运行”同时保留首次失败和修正后通过，并提供可展开的有界日志。
- 文件工件保留路径、行号、只读状态和 Agent 定位高亮；代码范围只生成可移除上下文，不触发发送。
- 右栏工件切换没有改变 Agent 消息；刷新以及切到论文研读再返回后，项目会话仍恢复完成状态和最后的“运行”工件。

### 响应式与交互复核

- 1440 × 1024：`innerWidth = scrollWidth = 1440`，左栏 240 px、Agent 460 px；折叠 Agent 后可重新唤起，拖拽实测可从 460 px 调到 530 px。
- 1180 px：`innerWidth = scrollWidth = 1180`，两栏继续容纳 Agent 输入和运行证据，无页面级横向溢出。
- 390 × 844：`innerWidth = scrollWidth = 390`；一次只显示项目、Agent、工件或技能。底部主导航高 59 px，项目工件 Tab 和头部核心按钮均为 44 px。
- 完整点击路径已验证：绑定预置项目 → 显式提交任务 → 查看计划 → 执行 → 取消仍停留审阅 → 哈希绑定确认 → 查看预览 → 首次验证失败 → 重试通过 → 切换论文会话 → 返回并恢复。
- 新开的浏览器页无 `warn` 或 `error` 控制台日志；未发现仍需修复的 P0、P1 或 P2 视觉问题。

### 验证结果

- `npm test`：最终全量回归结果见本文件末次验收记录。
- `npm run build`：通过；仅保留既有的 Vite 大 chunk 提示，不影响本次前端合同。

### 末次验收记录

- `npm test`：301/301 通过。
- `npm run build`：通过；仅有既存的大 chunk 提示。

final result: passed

## 工作类型与桌面工作区修正（2026-07-25）

### 本轮事实源与范围

- 用户确认的结构顺序为：先在左上角选择 `正常工作 / 论文精读`，再查看和管理该类型下的项目与会话。
- 调整前桌面截图：`/private/tmp/pi-agent-desktop-mode-layout-before.png`（1280 × 720）。
- 默认完整 Agent 工作区：`/private/tmp/pi-agent-desktop-mode-layout-final-1280.png`（1280 × 720）。
- 显式展开右侧工件：`/private/tmp/pi-agent-desktop-artifact-open-final-1280.png`（1280 × 720）。
- 添加项目两种路径：`/private/tmp/pi-agent-desktop-project-chooser-final-1280.png`（1280 × 720）。
- 本轮验收范围仅为电脑端；按用户要求未执行移动端视觉 QA。

### 可见结构与交互

- 左上角新增工作类型分段选择；两种类型拥有各自的项目、会话选择和恢复状态，切换本身不会调用模型。
- 默认只展开左栏与 Agent 工作区，Agent 占满剩余宽度；右侧工件通过 `打开工件 / 收起工件` 显式切换。
- 左栏收起入口只保留工作区头部一个；默认态和收起后的恢复态都不存在重复按钮。
- `添加项目` 提供 `选择本地文件夹` 和 `新建项目文件夹` 两条清晰路径；前端只显示安全的 `rootLabel`。
- 用户新增项目可以从当前工作类型移除，移除动作不删除本地文件；预置项目不可移除。
- 工作类型、项目、会话和右侧工件恢复均使用前端状态；真实目录选择、目录创建、目录索引和 Agent runtime 仍属于后续服务端替换范围。

### 桌面验证

- 1280 × 720 默认态：`scrollWidth = clientWidth = 1280`，右侧工件为关闭状态，左栏切换按钮计数为 1。
- 1440 × 1024 与 1180 × 844 均未出现页面级横向溢出。
- 已点击验证：正常工作与论文精读互相切换并恢复各自会话；打开和收起右侧工件；左栏收起与恢复；两种添加项目路径；新增项目移除。
- 新开的最终预览页控制台 `warn/error` 为 0。
- `npm test`：302/302 通过。
- `npm run build`：通过；仅保留既有的 Vite 大 chunk 提示。
- `git diff --check`：通过。

final result: passed
